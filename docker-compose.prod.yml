version: "3.9"

services:
  kiosk-backend:
    image: kiosk-backend:latest
    container_name: kiosk-backend
    ports:
      - "8081:8080"
    env_file:
      - .env.prod
    restart: unless-stopped
