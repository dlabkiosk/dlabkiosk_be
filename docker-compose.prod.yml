version: "3.9"

services:
  kiosk-backend:
    image: deasung-kiosk:latest
    container_name: deasung-kiosk
    ports:
      - "8081:8080"
    env_file:
      - .env.prod
    restart: unless-stopped
