spring:
  config:
    import: optional:file:.env[.properties]

  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  flyway:
    enabled: true
    repair-on-migrate: true

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}



  jwt:
    secret: ${JWT_SECRET}
    access-expiration: ${JWT_ACCESS_EXPIRATION}
    refresh-expiration: ${JWT_REFRESH_EXPIRATION}
    kiosk-expiration: ${KIOSK_TOKEN_EXPIRATION}

decorator:
  datasource:
    p6spy:
      enable-logging: true


cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET}
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-northeast-2

server:
  port: 8080



    #dsa:
    # api:
    #  base-url: https://devapi.dshw.co.kr
  # connect-timeout: 10000
  # read-timeout: 10000



sync:
  student:
    cron: "0 0 4 * * *"

dsa:
  api:
    base-url: https://devapi.dshw.co.kr
    seat-base-url: https://api.dshw.co.kr
    connect-timeout: 10000
    read-timeout: 10000
